apiVersion: v1
kind: Service
metadata:
  name: feline-registry
  labels:
    app: feline-registry
spec:
  selector:
    app: feline-registry
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
  type: ClusterIP
